# Base de données (valeurs par défaut si non définies)
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=hyrox_db_staging

# JWT (obligatoire)
JWT_SECRET=your-staging-jwt-secret-key
JWT_EXPIRATION=7d

# CORS (par défaut: http://localhost)
CORS_ORIGIN=http://localhost:8080

# Nginx (par défaut: 8080)
NGINX_PORT=8080
