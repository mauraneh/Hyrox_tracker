# Base de données (valeurs par défaut si non définies)
DB_USER=postgres.lxalnuoxvjhalyzqziyo
DB_PASSWORD=mynameisSHINNY15@
DB_NAME=postgres

# JWT (obligatoire)
JWT_SECRET=dev-secret-key-change-in-production
JWT_EXPIRATION=7d

# CORS (par défaut: http://localhost)
CORS_ORIGIN=http://localhost:4200

# Nginx (par défaut: 8080)
NGINX_PORT=8080
